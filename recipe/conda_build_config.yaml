# Keep this in sync with the one in https://github.com/eclipse-zenoh/zenoh/blob/main/rust-toolchain.toml
# for the given packaged release
# This is enforced by check_rust_compiler_version.py during the build.
rust_compiler_version:
    - '1.97.1'
